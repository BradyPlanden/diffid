name: CI

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  LLVM_VERSION: "18.1"
  PYTHON_VERSION: "3.12"

jobs:
  wheels:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - runner: ubuntu-24.04
            target: x86_64
          - runner: windows-latest
            target: x64
          - runner: macos-15-intel
            target: x86_64
          - runner: macos-latest
            target: aarch64

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v3

      # LLVM for Windows
      - name: Install LLVM (Windows)
        if: runner.os == 'Windows'
        uses: KyleMayes/install-llvm-action@v2
        with:
          version: ${{ env.LLVM_VERSION }}
          directory: ${{ runner.temp }}/llvm

      # LLVM for macOS via homebrew
      - name: Install LLVM (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install llvm@18
          echo "LLVM_SYS_181_PREFIX=$(brew --prefix llvm@18)" >> $GITHUB_ENV
          echo "LIBCLANG_PATH=$(brew --prefix llvm@18)/lib" >> $GITHUB_ENV

      # Linux: LLVM for tests (wheel built in container)
      - name: Install LLVM (Linux tests)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-18-dev libclang-18-dev libzstd-dev libpolly-18-dev

      # Windows build
      - name: Build wheels (Windows)
        if: runner.os == 'Windows'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
        env:
          LLVM_SYS_181_PREFIX: ${{ runner.temp }}/llvm
          LIBCLANG_PATH: ${{ runner.temp }}/llvm/bin
          LIBCLANG_STATIC_PATH: ${{ runner.temp }}/llvm/lib
          LLVM_CONFIG_PATH: ${{ runner.temp }}/llvm/bin/llvm-config.exe

      # macOS build
      - name: Build wheels (macOS)
        if: runner.os == 'macOS'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'

      # Linux build
      - name: Build wheels (Linux)
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
          manylinux: '2_28'
          before-script-linux: |
            yum install -y ncurses-compat-libs zlib-devel libzstd-devel libxml2-devel ncurses-devel libffi-devel
            curl -sL https://github.com/llvm/llvm-project/releases/download/llvmorg-18.1.8/clang+llvm-18.1.8-x86_64-linux-gnu-ubuntu-18.04.tar.xz | tar -xJ -C /opt
            mv /opt/clang+llvm-18.1.8-x86_64-linux-gnu-ubuntu-18.04 /opt/llvm-18
            export LLVM_SYS_181_PREFIX=/opt/llvm-18
            export LIBCLANG_PATH=/opt/llvm-18/lib
            GCC_VERSION=$(gcc -dumpversion)
            export BINDGEN_EXTRA_CLANG_ARGS="--sysroot=/usr -I/opt/rh/gcc-toolset-14/root/usr/lib/gcc/x86_64-redhat-linux/${GCC_VERSION}/include"

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.platform.runner }}-${{ matrix.platform.target }}
          path: dist

      # Tests - Windows
      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        run: |
          uv sync --group dev
          uv run pytest -v
        env:
          RUSTC_WRAPPER: ""
          LLVM_SYS_181_PREFIX: ${{ runner.temp }}/llvm
          LIBCLANG_PATH: ${{ runner.temp }}/llvm/bin

      # Tests - macOS
      - name: Run tests (macOS)
        if: runner.os == 'macOS'
        run: |
          uv sync --group dev
          uv run pytest -v
        env:
          RUSTC_WRAPPER: ""

      # Tests - Linux
      - name: Run tests (Linux)
        if: runner.os == 'Linux'
        run: |
          uv sync --group dev
          uv run pytest -v
        env:
          RUSTC_WRAPPER: ""
          LLVM_SYS_181_PREFIX: /usr/lib/llvm-18
          LIBCLANG_PATH: /usr/lib/llvm-18/lib

  sdist:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: dtolnay/rust-toolchain@stable

      - name: Install LLVM
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-18-dev libclang-18-dev libzstd-dev libpolly-18-dev

      - name: Generate stubs & build sdist
        run: cargo run -p chronopt-py --no-default-features --features stubgen --bin generate_stubs
        env:
          LLVM_SYS_181_PREFIX: /usr/lib/llvm-18
          LIBCLANG_PATH: /usr/lib/llvm-18/lib

      - uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist