

var1 = Normal(0.5, 0.1)
var2 = Normal(0.4, 0.1)

# Construct some data
# Build a probabilistic model
# Perform model inference


# Use identified model
observation = fit_model.random_sample()  # Take an observation
